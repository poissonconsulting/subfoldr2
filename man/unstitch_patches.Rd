% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{unstitch_patches}
\alias{unstitch_patches}
\title{Unstitch patches}
\usage{
unstitch_patches(x)
}
\arguments{
\item{x}{An object of class \code{"ggplot"} and generally also \code{"patchwork"}.}
}
\description{
Separates patches from a multi-panel plot created via \code{{patchwork}} into a
list of the individual plots.
}
\details{
Iteratively splits \code{patchwork} objects into smaller elements until a list of
simple \code{ggplot} plots is created.
If \code{x} is a \code{ggplot} object but not a \code{patchwork} object, it is simply
returned.
}
\examples{
p <- ggplot2::ggplot() + ggplot2::ggtitle("1")
# combine into a patchwork then split it back into its individual plots
if (requireNamespace("patchwork", quietly = TRUE)) {
  pw <- patchwork::wrap_plots(p, p)
  length(subfoldr2:::unstitch_patches(pw))
}
}
\keyword{internal}
