% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/save.R
\name{sbf_save_plot}
\alias{sbf_save_plot}
\title{Save Plot}
\usage{
sbf_save_plot(
  x = ggplot2::last_plot(),
  x_name = substitute(x),
  sub = sbf_get_sub(),
  main = sbf_get_main(),
  caption = "",
  report = TRUE,
  tag = "",
  units = "in",
  width = NA,
  height = width,
  dpi = 300,
  limitsize = TRUE,
  csv = 1000L
)
}
\arguments{
\item{x}{The ggplot object to save.}

\item{x_name}{A string of the name.}

\item{sub}{A string specifying the path to the sub folder (by default the
current sub folder).}

\item{main}{A string specifying the path to the main folder (by default the
current main folder)}

\item{caption}{A string of the caption.}

\item{report}{A flag specifying whether to include in a report.}

\item{tag}{A string of the tag.}

\item{units}{A string of the units. Can be "in" (default) or "mm" or "cm".}

\item{width}{A number of the plot width in inches.}

\item{height}{A number of the plot width in inches.}

\item{dpi}{A number of the resolution in dots per inch.}

\item{limitsize}{When \code{TRUE} (the default), \code{ggsave()} will not
save images larger than 50x50 inches, to prevent the common error of
specifying dimensions in pixels.}

\item{csv}{A count specifying the maximum number of rows to save as a csv
file.}
}
\description{
Saves a ggplot object.
By default it saves the last plot to be modified or created.
}
\details{
The function saves:
\enumerate{
\item A \code{png} file of the plot.
\item An \code{rda} file of the plot metadata.
\item An \code{rds} file of the plot.
\item A \code{csv} of the data passed to \code{ggplot()}, provided it has at least one row
and no more than \code{csv} rows. If \code{x} is a \code{patchwork} object, only the data
for the first patch is saved, to maintain compatibility with previous
versions.
Uninformative columns are dropped via \code{tidyplus::drop_uninformative_columns()}
using default arguments.
\item An \code{xlsx} workbook with a sheet for the data passed to each \code{ggplot()}
call and a sheet for each geom layer. Sheets are labelled \verb{<p>_<l>_<geom>},
where \verb{<p>} is the patch index (1 for a simple \code{ggplot}), \verb{<l>} is the layer
index (\code{0} for the \code{ggplot()} data), and \verb{<geom>} is the layer type (e.g.
\code{point}, \code{line}). Layers with more than \code{csv} rows are omitted.
}

The \code{csv} and \code{xlsx} files are named using the \code{x_name} argument. Not
specifying \code{x} overwrites existing files that used \code{x_name = "plot"}.
}
\examples{
\dontrun{
require(ggplot2)
require(patchwork)
ggplot(mtcars) +
  geom_line(aes(mpg, cyl, color = cyl), mtcars) +
  ggtitle("1")
sbf_save_plot()

p_patches <-
  ((ggplot(mtcars) +
      geom_line(aes(mpg, cyl, color = cyl), mtcars) +
      ggtitle("1")) +
     (ggplot() +
        geom_line(aes(Sepal.Length, Petal.Length), iris) +
        ggtitle("2"))) /
  ((ggplot() +
      geom_point(aes(mpg, cyl, color = cyl), mtcars) +
      ggtitle("3")) +
     ((ggplot(iris) +
         geom_point(aes(Sepal.Length, Petal.Length)) +
         ggtitle("4")) +
        (ggplot() +
           geom_point(aes(Sepal.Length, Petal.Length, color = Species),
                      iris) +
           ggtitle("5") +
           theme(legend.position = "none"))))
p_patches
sbf_save_plot(p_patches)
}

}
\seealso{
Other save functions:
\code{\link[=sbf_basename_sans_ext]{sbf_basename_sans_ext()}},
\code{\link[=sbf_save_aws_files]{sbf_save_aws_files()}},
\code{\link[=sbf_save_block]{sbf_save_block()}},
\code{\link[=sbf_save_data]{sbf_save_data()}},
\code{\link[=sbf_save_data_to_db]{sbf_save_data_to_db()}},
\code{\link[=sbf_save_data_to_pg]{sbf_save_data_to_pg()}},
\code{\link[=sbf_save_datas]{sbf_save_datas()}},
\code{\link[=sbf_save_datas_to_db]{sbf_save_datas_to_db()}},
\code{\link[=sbf_save_db_metatable_descriptions]{sbf_save_db_metatable_descriptions()}},
\code{\link[=sbf_save_db_to_workbook]{sbf_save_db_to_workbook()}},
\code{\link[=sbf_save_excel]{sbf_save_excel()}},
\code{\link[=sbf_save_excels]{sbf_save_excels()}},
\code{\link[=sbf_save_gpkg]{sbf_save_gpkg()}},
\code{\link[=sbf_save_gpkgs]{sbf_save_gpkgs()}},
\code{\link[=sbf_save_number]{sbf_save_number()}},
\code{\link[=sbf_save_numbers]{sbf_save_numbers()}},
\code{\link[=sbf_save_object]{sbf_save_object()}},
\code{\link[=sbf_save_objects]{sbf_save_objects()}},
\code{\link[=sbf_save_png]{sbf_save_png()}},
\code{\link[=sbf_save_spatial]{sbf_save_spatial()}},
\code{\link[=sbf_save_spatials]{sbf_save_spatials()}},
\code{\link[=sbf_save_string]{sbf_save_string()}},
\code{\link[=sbf_save_strings]{sbf_save_strings()}},
\code{\link[=sbf_save_table]{sbf_save_table()}},
\code{\link[=sbf_save_window]{sbf_save_window()}},
\code{\link[=sbf_save_workbook]{sbf_save_workbook()}}
}
\concept{save functions}
